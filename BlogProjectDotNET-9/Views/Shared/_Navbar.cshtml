<nav class="navbar navbar-expand-lg bg-light" data-bs-theme="light">

    <div class="container-fluid">
        <a class="navbar-brand" href="#">Navbar</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor03">

            <ul class="navbar-nav me-auto">

               
                @if (User.IsInRole("Admin"))
                {
                    <li class="nav-item">
                        <a class="nav-link text-dark" asp-controller="Admin" asp-action="Dashboard">Dashboard</a>
                    </li>
                }




                @if (User.IsInRole("Author"))
                {
                    <li class="nav-item">
                        <a class="nav-link text-dark" asp-controller="Post" asp-action="MyPosts">My Posts</a>
                    </li>
                }

                <li class="nav-item">
                    <a class="nav-link active" asp-action="Index" asp-controller="Post">
                        Home
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" asp-action="Index" asp-controller="Post">
                        Categories
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">About</a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Dropdown</a>
                    <div class="dropdown-menu">
                        <a class="dropdown-item" href="#">Another action</a>
                        <a class="dropdown-item" href="#">Something else here</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="#">Separated link</a>
                    </div>
                </li>
                
            </ul>
            <form class="d-flex">
                <input class="form-control me-sm-2" type="search" placeholder="Search">
                <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
            </form>
            @if (User.Identity.IsAuthenticated == false)
            {

                <div class="d-flex gap-2">
                    <a asp-controller="Account" asp-action="Login" class="btn btn-primary text-white">Login</a>
                    <a asp-controller="Account" asp-action="Register" class="btn btn-secondary text-white">Register</a>
                </div>

            }
            else
            {
                <div class="d-flex gap-2">
                    <a asp-controller="Account" asp-action="Logout" class="btn btn-danger text-white">Logout</a>
                </div>
            }

        </div>
    </div>
</nav>